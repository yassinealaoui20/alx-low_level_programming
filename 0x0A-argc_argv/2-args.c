#include <stdio.h>
/**
 * main - Entry point
 * @argc: an integer
 * @argv: array pointers to char
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int = 0;
	while (i < argc)
	{
		printf("%S\n", argv[i]);
		i++;
	}
	return (0);
}
