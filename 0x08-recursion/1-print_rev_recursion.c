#include "main.h"
/**
 * _puts_recursion - prints a string follwed by '\n'
 * @s: a pointer to the first C in array in memory
 *
 * Return: 0
 */
void _print_rev_recursion(char *s)
{
	if (*s > '\0')
	{
		_print_rev_recursio--- - 1);
		_putchar(*s);
	}
}

